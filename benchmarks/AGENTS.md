# Benchmark inspection

Start with `SUMMARY.md` or `latest.json`. Read only the raw report(s) relevant to
the question; avoid bulk-reading `runs/`, `baselines/`, or generated full views.
Raw reports preserve all history. Generate detailed views when needed with
`python3 tools/benchmarks.py summary --full`; do not hand-edit derived files.
